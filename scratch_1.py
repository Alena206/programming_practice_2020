x = int(input(1))
import numpy as np
y = np.log((np.e * 1/(np.sin(x) + 1)) / (1.25 + 1/(x**15))) / np.log(1 + x**2)
print(y)
x = int(input(10))
import numpy as np
y = np.log((np.e * 1/(np.sin(x) + 1)) / (1.25 + 1/(x**15))) / np.log(1 + x**2)
print(y)x = int(input(1000))
import numpy as np
y = np.log((np.e * 1/(np.sin(x) + 1)) / (1.25 + 1/(x**15))) / np.log(1 + x**2)
print(y)

